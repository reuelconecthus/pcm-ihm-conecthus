{
  "name": "comunicacao-maquinas",
  "version": "1.0.0",
  "description": "Aplicacao para comunicacao TCP/IP com maquinas",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc && node scripts/copy-assets.cjs",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && electron .",
    "dev": "npm run start"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "electron": "^44.0.0",
    "nodemon": "^3.1.14",
    "typescript": "^7.0.2"
  }
}
